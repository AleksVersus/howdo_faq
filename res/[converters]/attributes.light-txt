==Атрибуты узлов==

start-head-level - начальный уровень заголовков для этого узла. Т.е. уровень заголовков в текущем узле не может превышать указанный (число).

:folder

:file

:head

anchor - якорь, идентификатор заголовка/секции, id
head-level - уровень заголовка исходя из разметки в файле (h1, h2 и т.п.)
real-head-level - действительный уровень заголовка (число)


:list-node

list-type - тип списка ul-list или ol-list